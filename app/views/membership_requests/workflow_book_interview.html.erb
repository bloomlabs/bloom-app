<%= render 'workflow_header' %>

<%= form_tag do %>
    <div class="field">
      <p>Please specify some times in the two weeks before your nominated start date you'll be available for an interview</p>
      <p>We will be in touch to confirm your interview time</p>
      <%= text_area_tag(:interview_book_info) %>
    </div>

    <%= submit_tag('Submit') %>
<% end %>

<%= render 'workflow_footer' %>